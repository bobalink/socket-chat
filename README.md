##Running the app  

`npm run start` to start application. 

any browser window that goes to `localhost:3000` will join the chat room. 

#commands

`/nickname <new_name>`

Able to change your ID to a nickname. nickname can not have any spaces. 